{
  "name": "@sns-agent/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "cf:dev": "pnpm dlx wrangler dev --config wrangler.toml",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.7.6"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.4"
  }
}
