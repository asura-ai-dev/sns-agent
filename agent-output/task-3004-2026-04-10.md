# Task 3004 Handoff - Web UI: Next.js 初期化とレイアウト・テーマ

## Spec Alignment

spec.md AC-15（デスクトップ 1280px 以上とモバイル 375px 以上の両方で操作可能）に対応。Web UI 基本画面（P0-8）の基盤として、Next.js + DaisyUI + Tailwind CSS のレイアウトシェルとカスタムテーマを構築した。

## Phase

implementation - complete

## completed

- Next.js App Router 設定（standalone 出力、transpilePackages に @sns-agent/ui, @sns-agent/sdk, @sns-agent/config）
- Tailwind CSS v4 + DaisyUI v5 設定（postcss.config.mjs, globals.css）
- カスタムテーマ `sns-agent` を oklch カラーで定義（primary: #06C755 相当、secondary: #111111 相当、accent: #FF7A59 相当、surface: #FFFDF8 相当、全セマンティックカラー）
- AppShell.tsx: drawer + drawer-content 構成で Sidebar + Header + Main を実装
- Sidebar.tsx: 8つのナビゲーション項目（Phosphor Icons 使用）、アクティブ状態表示、デスクトップ（lg+ = 1024px）で常時表示、モバイルで drawer
- Header.tsx: ワークスペース名、通知ベル（未読インジケータ付き）、ユーザーアバター + ドロップダウンメニュー
- ルートレイアウト: data-theme="sns-agent" 適用、メタデータ設定
- ダッシュボードレイアウトグループ: (dashboard)/layout.tsx with AppShell ラップ、認証チェックプレースホルダ
- ダッシュボードページ: プレースホルダカード（投稿数、予約数、API使用量、アクティブ接続）
- フォント: DM Sans（本文）+ Fraunces（ディスプレイ）を Google Fonts から読み込み
- @phosphor-icons/react インストール済み
- barrel export (components/layout/index.ts)

## in_progress

- なし

## blocked

- なし

## tests_run

- TypeScript 型チェック (tsc --noEmit): pass
- Next.js dev サーバー (Turbopack): 起動成功、HTTP 200 応答確認
- HTML 出力に data-theme="sns-agent"、drawer 構造、Sidebar/Header コンポーネントがすべて含まれることを curl で確認

## known_gaps

- `next build` (webpack) が Tailwind v4 + DaisyUI v5 の CSS 処理で非常に遅い（Turbopack では正常動作）。本番ビルドは Turbopack build の安定版を待つか、Webpack 側の設定調整が必要な可能性がある
- Google Fonts の @import は外部依存。オフライン環境では next/font に切り替えが必要

## next_step

- 後続タスクでダッシュボードの実データ接続
- 認証チェックの実装（(dashboard)/layout.tsx のプレースホルダ）
- 各ルートページの実装（/posts, /calendar, /inbox 等）

## 重要判断

- Tailwind v4 + DaisyUI v5 を採用（v3 + v4 ではなく最新版）。CSS-in-CSS 方式（@plugin）でテーマ定義
- oklch カラースペースでテーマ色を定義（DaisyUI v5 のネイティブ形式）
- レスポンシブブレークポイント: lg (1024px) でデスクトップ/モバイル切り替え（チケット仕様通り）

## 変更ファイル

- apps/web/package.json - tailwindcss, daisyui, @phosphor-icons/react, postcss, @tailwindcss/postcss 追加
- apps/web/postcss.config.mjs - 新規作成
- apps/web/next.config.ts - standalone 出力追加
- apps/web/src/app/globals.css - 新規作成（Tailwind, DaisyUI テーマ, カスタム CSS）
- apps/web/src/app/layout.tsx - data-theme, globals.css import, フォント設定
- apps/web/src/app/(dashboard)/layout.tsx - 新規作成
- apps/web/src/app/(dashboard)/page.tsx - 新規作成
- apps/web/src/components/layout/AppShell.tsx - 新規作成
- apps/web/src/components/layout/Sidebar.tsx - 新規作成
- apps/web/src/components/layout/Header.tsx - 新規作成
- apps/web/src/components/layout/index.ts - 新規作成
