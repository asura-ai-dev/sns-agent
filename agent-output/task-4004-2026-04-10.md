# Task 4004: 予算ポリシー管理 — Handoff

## Spec Alignment

spec.md 主要機能 10「予算ポリシー」（ワークスペース/SNS/エンドポイント単位の上限設定と warn/require-approval/block による超過時アクション）および評価観点「予算超過時に設定されたポリシーが適用されるか」に対応する。design.md 3.1 (budget_policies), 4.2 (/api/budget) に準拠。

## Phase

Phase 4 — 全 SNS 対応 + 使用量（予算制御レイヤ）

## Completed

- `packages/core/src/policies/budget.ts` を新規作成
  - `evaluateBudgetPolicy(deps, { workspaceId, platform, endpoint, additionalCost, now? })`
  - scope 優先度: endpoint > platform > workspace（同スコープ内は createdAt 昇順）
  - 現在期間 (daily / weekly / monthly) の消費額を `usageRepo.aggregate` で集計
  - 80% 到達で `warning=true`（ポリシー action に関わらず）
  - 超過時は `policy.actionOnExceed` を返し、`block` のみ `allowed=false`
  - `getPeriodStart` / `getPeriodEnd`（UTC 基準、ISO 週対応）をエクスポート
- `packages/core/src/usecases/budget.ts` を新規作成
  - `listPolicies(deps, workspaceId)`
  - `createPolicy(deps, input)` — scopeType 別 scopeValue バリデーション、limit > 0 チェック
  - `updatePolicy(deps, workspaceId, policyId, input)` — workspace 一致チェック、partial update
  - `deletePolicy(deps, workspaceId, policyId)` — workspace 一致チェック
  - `getBudgetStatus(deps, workspaceId, now?)` — 全ポリシーの consumed / limit / percentage / warning / exceeded を返す
- `packages/core/src/interfaces/repositories.ts` を拡張
  - `BudgetPolicyRepository.findById(id)` を追加
  - `UsageRepository.aggregate` に `endpoint?: string` オプションを追加（scope=endpoint ポリシー向け）
- `packages/db/src/repositories/budget-policy-repository.ts` を拡充
  - `findById` を実装
- `packages/db/src/repositories/usage-repository.ts` を拡充
  - `aggregate` の `endpoint` フィルタ条件を追加
- `packages/core/src/policies/index.ts` / `packages/core/src/index.ts` のバレル export を更新
  - `evaluateBudgetPolicy`, `BudgetEvaluation`, `getBudgetPeriodStart/End` を公開
  - `listBudgetPolicies`, `createBudgetPolicy`, `updateBudgetPolicy`, `deleteBudgetPolicy`, `getBudgetStatus` を公開
- `apps/api/src/routes/budget.ts` を全面実装
  - `GET /api/budget/policies` (`budget:read`)
  - `POST /api/budget/policies` (`budget:manage`)
  - `PATCH /api/budget/policies/:id` (`budget:manage`)
  - `DELETE /api/budget/policies/:id` (`budget:manage`)
  - `GET /api/budget/status` (`budget:read`)
  - 認証ミドルウェアと監査ミドルウェアは `apps/api/src/app.ts` で既に `/api/budget/*` に適用済み
- `packages/core/src/usecases/post.ts` を拡張
  - `PostUsecaseDeps` に `budgetPolicyRepo?`, `approvalRepo?`, `auditRepo?` を追加（後方互換、未設定時はチェックをスキップ）
  - `publishPostChecked(deps, workspaceId, postId, options?)` を追加 — 予算ポリシー評価付き公開
    - `block`: `BudgetExceededError` を throw
    - `require-approval`: `createApprovalRequest` で承認リクエスト作成、Post を `scheduled` 状態に更新、`approvalRequestId` を返す
    - `warn`: 続行、`budgetEvaluation` をレスポンスに含める
  - 旧 `publishPost(deps, workspaceId, postId)` は `publishPostChecked` を呼ぶ薄いラッパとして維持（後方互換）
- `apps/api/src/routes/posts.ts` を更新
  - `/api/posts/:id/publish` ハンドラで `publishPostChecked` を使用
  - レスポンスに `meta.budget`（BudgetEvaluation のシリアライズ）を含める
  - 予算 require-approval で承認作成時は 202 + `meta.requiresApproval=true, approvalId`
  - `buildDeps` で `DrizzleBudgetPolicyRepository`, `DrizzleApprovalRepository`, `DrizzleAuditLogRepository` を注入
- `packages/core/src/usecases/__tests__/budget.test.ts` を新規作成（16 テスト）

## In Progress

なし

## Blocked

なし

## Tests Run

- `pnpm --filter @sns-agent/core test` — 9 suites, 133 tests PASS（内、新規 budget.test.ts は 16 テスト）
  - evaluateBudgetPolicy: no-policy / scope priority / block / require-approval / 80% warn / out-of-period ignored
  - listPolicies / createPolicy / updatePolicy / deletePolicy / getBudgetStatus
- `pnpm --filter @sns-agent/core build` — PASS
- `pnpm --filter @sns-agent/db build` — PASS
- `pnpm --filter @sns-agent/api build` — PASS
- `pnpm -r --parallel build` — 全 13 ワークスペース PASS（web の Next.js static 生成含む）
- `pnpm -r --parallel test` — core 133 / llm 24 / provider-instagram 28 / provider-x 32 / provider-line 16 全 PASS

## Known Gaps

- `packages/db` に実 DB を叩く integration test がないため、Drizzle の findById / aggregate (endpoint filter) は SQL レベルの検証をしていない（vitest の mock で usecase レベルの検証のみ）。本番相当の E2E は別タスクで対応。
- `/api/budget/status` のレスポンスで期間（daily/weekly/monthly）の切替 UI は未実装（本タスクは API のみ、UI 側は別タスク）。
- `publishPostChecked` の budget 評価は `post.publish` エンドポイントに限定（コストは `estimateCost(platform, "post.publish", 1)` から取得）。複数回/カスタム endpoint の運用は後続で拡張可能。

## Next Step

1. `/verify tasks/phase-4/task-4004.md` を Orchestrator 側で実行し done_when 条件を検証する
2. Phase 4 の他チケットとの結合（評価フェーズ）で、投稿公開フローが予算 block / require-approval / warn の 3 ケースで期待通りに動くかを evaluator が確認する
3. Phase 4 完了後、web UI に budget 画面（/settings/budget 等）を追加する別タスクを起票する（スコープ外）

## 主な変更ファイル

- `packages/core/src/policies/budget.ts` (new)
- `packages/core/src/policies/index.ts`
- `packages/core/src/usecases/budget.ts` (new)
- `packages/core/src/usecases/post.ts`
- `packages/core/src/usecases/__tests__/budget.test.ts` (new)
- `packages/core/src/interfaces/repositories.ts`
- `packages/core/src/index.ts`
- `packages/db/src/repositories/budget-policy-repository.ts`
- `packages/db/src/repositories/usage-repository.ts`
- `apps/api/src/routes/budget.ts`
- `apps/api/src/routes/posts.ts`
