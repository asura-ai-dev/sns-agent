# Task 1002: packages/config - 共通設定と環境変数スキーマ

## Spec Alignment

spec.md P0-1「モノレポ基盤構築」の一部として、全パッケージが参照する共通設定パッケージを実装。design.md セクション 5 の依存関係図で `packages/config <- 全パッケージが参照` に対応する。

## completed

- `packages/config/src/constants.ts` - 6 つの定数定義（Platform, Role, PostStatus, JobStatus, AccountStatus, BudgetAction）を as const 配列 + 型エクスポートとして実装
- `packages/config/src/env.ts` - Zod v4 による環境変数スキーマ（DATABASE_URL, ENCRYPTION_KEY, API_PORT, WEB_URL, NODE_ENV）と `getConfig()` / `resetConfigCache()` 関数を実装
- `packages/config/src/index.ts` - 全エクスポートを集約
- `.env.example` をプロジェクトルートに作成（全環境変数のサンプルとコメント付き）
- zod と @types/node を packages/config の依存に追加
- TypeScript ビルド成功確認
- ランタイム動作確認（定数値、getConfig パース、バリデーションエラー）

## in_progress

- なし

## blocked

- なし

## tests_run

- `pnpm --filter @sns-agent/config build` - 成功
- Node.js ランタイムテスト（定数エクスポート確認、getConfig 正常パース、DATABASE_URL 欠落時のバリデーションエラー発生確認） - 全て成功

## known_gaps

- ENCRYPTION_KEY の本番必須チェックは `.refine()` で実装。`process.env.NODE_ENV` を直接参照しているため、`getConfig()` 呼び出し前に NODE_ENV が設定されている必要がある（スキーマのパース順序上、NODE_ENV はスキーマ側でデフォルト値を持つため、process.env の値で判定している）

## next_step

- 後続パッケージ（core, db 等）から `@sns-agent/config` を `workspace:*` で依存追加して利用開始
