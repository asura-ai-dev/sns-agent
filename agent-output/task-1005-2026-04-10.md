# Handoff: Task 1005 - packages/sdk TypeScript SDK 基盤

## Spec Alignment

spec.md の「CLI」「Web UI」「SDK」に対応。CLI と Web UI が共通の core use cases を SDK 経由で呼び出す基盤（AC-7, AC-8, AC-9 で SDK が JSON 出力の共通土台となる）。design.md セクション 4 の REST API エンドポイント・セクション 4.3 の共通レスポンス形式に準拠。

## Phase

Phase 1: 基盤構築

## completed

- `packages/sdk/src/errors.ts` - SdkError クラス（statusCode, code, message, details）と fromResponse ファクトリメソッド
- `packages/sdk/src/types.ts` - API リクエスト/レスポンス型（ApiResponse<T> { data, meta } / ApiErrorResponse { error }）、リソース別入出力型、core エンティティの re-export
- `packages/sdk/src/client.ts` - SnsAgentClient クラス（fetch ベース、Authorization/Content-Type/X-Idempotency-Key ヘッダ自動付与、get/post/patch/delete 汎用メソッド、accounts/posts/schedules/usage リソース別メソッド）
- `packages/sdk/src/index.ts` - 全エクスポートの集約
- `packages/sdk/package.json` - @sns-agent/core, @sns-agent/config を workspace:\* で依存追加
- TypeScript ビルド成功、ランタイムインポート・インスタンス化・メソッド存在・SdkError 生成を動作確認済み

## in_progress

なし

## blocked

なし

## tests_run

- `pnpm --filter @sns-agent/sdk build` - 成功（TypeScript コンパイル、型チェック通過）
- Node.js ランタイムインポートテスト - 成功（SnsAgentClient インスタンス化、全リソースメソッドの存在確認、SdkError.fromResponse の正常系・異常系検証）

## known_gaps

- ユニットテスト（vitest）は未作成。テストフレームワークのセットアップは別チケット依存
- fetch のモックテスト（実際の HTTP 通信のテスト）は未実装

## next_step

- /verify による done_when 検証（Orchestrator の責務）
- CLI パッケージ（task-1006）が本 SDK を利用して実装を進める
