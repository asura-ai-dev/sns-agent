# Task 1006 done_when

- `pnpm dev --filter @sns-agent/api` でサーバーが起動し、ポート 3001 でリッスンする
- `GET http://localhost:3001/api/health` が `{ "status": "ok" }` を返す（200）
- レスポンスヘッダに `X-Request-Id` が含まれる
- 存在しないエンドポイントへのリクエストが `{ "error": { "code": "...", "message": "..." } }` 形式で返る
- `apps/api/src/routes/` に accounts.ts, posts.ts, schedules.ts, usage.ts, budget.ts, llm.ts, skills.ts, agent.ts, audit.ts, approvals.ts, webhooks.ts が存在する
- `apps/api/src/middleware/` に request-id.ts, error-handler.ts, cors.ts が存在する
- `pnpm build --filter @sns-agent/api` がエラーなく完了する
