# Task 1005 done_when

- `packages/sdk/src/client.ts` に SnsAgentClient クラスが存在し、`baseUrl` と `apiKey` をコンストラクタ引数に取る
- SnsAgentClient に `get`, `post`, `patch`, `delete` の汎用 HTTP メソッドが実装されている
- SnsAgentClient にリソース別メソッド（accounts.list, posts.list, posts.create, schedules.list, usage.report 等）が定義されている
- `packages/sdk/src/types.ts` に API リクエスト/レスポンスの型定義が存在する
- レスポンス型が `{ data: T, meta?: { page, limit, total } }` と `{ error: { code, message, details? } }` 形式に準拠している
- `packages/sdk/src/errors.ts` に SdkError クラスが定義されている
- `pnpm build --filter @sns-agent/sdk` がエラーなく完了する
