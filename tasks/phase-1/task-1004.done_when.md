# Task 1004 done_when

- `packages/db/src/schema/` に design.md セクション 3.1 の全テーブル（13テーブル）の Drizzle スキーマ定義が存在する
- `packages/db/src/client.ts` が存在し、`DATABASE_URL` の値に応じて SQLite / PostgreSQL を切り替える `getDb()` 関数が export されている
- `DATABASE_URL=file:./test.db` で `getDb()` を呼んだ場合に SQLite 接続が得られる
- `packages/db/src/repositories/` に AccountRepository, PostRepository, ScheduledJobRepository の実装が存在する
- 各 Repository 実装が `@sns-agent/core` の対応するインターフェースを implements している
- `packages/db/drizzle.config.ts` が存在する
- `pnpm build --filter @sns-agent/db` がエラーなく完了する
- `packages/db/package.json` の scripts に `db:generate`, `db:push` が定義されている
