# Task 1001 done_when

- `pnpm-workspace.yaml` が存在し、`apps/*` と `packages/*` を定義している
- `turbo.json` が存在し、`build`, `dev`, `lint`, `test` パイプラインが定義されている
- 全 13 パッケージ/アプリ（apps/web, apps/api, packages/core, packages/db, packages/sdk, packages/cli, packages/ui, packages/llm, packages/skills, packages/provider-x, packages/provider-line, packages/provider-instagram, packages/config）の `package.json` と `tsconfig.json` が存在する
- 各 `package.json` の `name` が `@sns-agent/<パッケージ名>` 形式になっている
- `pnpm install` がエラーなく完了する
- ルートの `tsconfig.json` が base config として存在し、各パッケージが `extends` で参照している
- ESLint + Prettier の設定ファイルがルートに存在する
- `pnpm lint` がルートから実行可能である（エラーなし）
