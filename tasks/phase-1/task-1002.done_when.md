# Task 1002 done_when

- `packages/config/src/env.ts` が存在し、Zod スキーマで環境変数をバリデーションしている
- `packages/config/src/constants.ts` が存在し、Platform, Role, PostStatus, JobStatus, AccountStatus, BudgetAction の型と値が定義されている
- `getConfig()` を呼び出すと型安全な設定オブジェクトが返る
- `DATABASE_URL` 未設定時に `getConfig()` がエラーをスローする
- ルートに `.env.example` が存在し、全環境変数のサンプルが記載されている
- `pnpm build --filter @sns-agent/config` がエラーなく完了する
- 他パッケージから `import { Platform, Role, getConfig } from "@sns-agent/config"` で参照可能である
