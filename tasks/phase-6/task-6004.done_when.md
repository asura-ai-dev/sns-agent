# Task 6004 done_when

- API 統合テストが 10 シナリオ以上存在し、全て pass する
- アカウント接続 -> 投稿作成 -> 公開の E2E フローがテストで pass する
- RBAC テスト（viewer が書き込み操作で 403）が pass する
- 予算チェックテスト（block ポリシーで 403）が pass する
- 承認フローテスト（agent が公開 -> 承認 -> 実行）が pass する
- CLI 統合テストが 5 シナリオ以上存在し、全て pass する
- `sns accounts list --json` のテストが pass する
- `sns post create` のテストが pass する
- Web UI E2E テスト（Playwright）が 5 シナリオ以上存在し、全て pass する
- `pnpm test` がルートから実行して全テスト pass する
- `pnpm build` が全パッケージでエラーなく完了する
- 外部 API がモック化されており、テストがネットワーク不要で実行可能である
