# Task 4003 done_when

- `POST` で投稿が公開された際に usage_records テーブルにレコードが記録される
- `GET /api/usage?period=monthly&platform=x` が月次の X 使用量レポートを返す
- レポートに requestCount, successRate, estimatedCost が含まれている
- `GET /api/usage/summary` が今月のサマリ（合計コスト、リクエスト数、成功率、SNS 別内訳）を返す
- daily, weekly, monthly の 3 つの期間で集計できる
- platform フィルタで特定 SNS のみの集計が取得できる
- `packages/config/src/cost-table.ts` に推定コスト単価が定義されている
- `pnpm build` がエラーなく完了する
