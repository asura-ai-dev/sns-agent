# Task 4002 done_when

- `packages/provider-instagram/src/index.ts` が SocialProvider インターフェースを実装するクラスを export している
- `getCapabilities()` が Instagram の capability オブジェクトを返し、textPost が false になっている
- `validatePost` がテキストのみ投稿（画像なし）をバリデーションエラーで拒否する
- `validatePost` がテキスト 2,200 文字超過時にバリデーションエラーを返す
- `publishPost` が Instagram Graph API の Container 作成 -> メディア公開の 2 ステップを実行する
- OAuth（Facebook Login 経由）の認可 URL 生成、コード交換、long-lived token リフレッシュが実装されている
- Instagram Webhook の署名検証が実装されている
- `apps/api/src/providers.ts` に Instagram プロバイダが登録されている
- `pnpm build --filter @sns-agent/provider-instagram` がエラーなく完了する
