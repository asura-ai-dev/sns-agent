# Task 4001 done_when

- `packages/provider-line/src/index.ts` が SocialProvider インターフェースを実装するクラスを export している
- `getCapabilities()` が LINE の capability オブジェクトを返す
- `validatePost` がテキスト 5,000 文字超過時にバリデーションエラーを返す
- `publishPost` が LINE Messaging API の push message を呼び出す実装になっている
- `deletePost` が LINE 非対応として ProviderError を返す
- LINE Webhook の署名検証が実装されている
- `apps/api/src/providers.ts` に LINE プロバイダが登録されている
- `POST /api/webhooks/line` で LINE Webhook を受信できる
- `pnpm build --filter @sns-agent/provider-line` がエラーなく完了する
