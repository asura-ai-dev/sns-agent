# Task 5007 done_when

- `openai-codex` provider を LLM route で保存できる
- `packages/llm/src/adapters/openai-codex.ts` が存在する
- workspace ごとの provider credentials 保存先が存在する
- OAuth 接続開始 / callback / status / disconnect の API が存在する
- `/settings/llm` で `openai-codex` の接続状態を確認できる
- `/settings/llm` から `openai-codex` の接続 / 再接続 / 切断ができる
- Agent Gateway が `openai-codex` route を解決して実行できる
- 既存 `openai` API キー経路は壊れていない
- 認証切れ時に `wire offline` ではなく再認証が必要だと分かる表示になる
- `pnpm --filter @sns-agent/api build` が通る
- `pnpm --filter @sns-agent/web typecheck` が通る
- 既存の LLM / Agent Gateway 系テストが回帰していない
