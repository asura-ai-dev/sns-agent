# Task 5001 done_when

- `packages/llm/src/adapters/openai.ts` が OpenAI API の chat completions を呼べる実装になっている
- `packages/llm/src/adapters/anthropic.ts` が Anthropic API の messages を呼べる実装になっている
- 両アダプタがストリーミング（SSE 用の async iterator）に対応している
- `resolveLlmRoute` が platform + action の優先度順でルートを解決する
- メインモデルの呼び出し失敗時にフォールバックモデルで再試行される
- LLM 呼び出し時に usage_records にトークン数と推定コストが記録される
- `GET /api/llm/routes` がルート一覧を返す
- `POST /api/llm/routes` でルート（provider, model, platform, action 等）を作成できる
- admin 未満のロールがルート作成/更新/削除を試みると 403 が返る
- `pnpm build --filter @sns-agent/llm` がエラーなく完了する
