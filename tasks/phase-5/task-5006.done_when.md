# Task 5006 done_when

- `apps/api/src/routes/agent.ts` に placeholderInvoker が存在しない（grep で "placeholder" が出ない）
- `grep -n "createPost\|schedulePost\|listPosts\|listAccounts" apps/api/src/routes/agent.ts` で少なくとも 4 件ヒット
- `executeInvoker` がビルトイン skill action（list_accounts, list_posts, create_post, schedule_post）を core use case に委譲している
- 未知の action 名は `{ status: "unsupported_action" }` を返す
- `pnpm build --filter @sns-agent/api` がエラーなく完了する
- `pnpm test --filter @sns-agent/core` が全 PASS する（回帰なし）
