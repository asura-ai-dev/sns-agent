# Task 3003 done_when

- `sns usage report --platform x --range month --json` がコマンドとして存在し、API を呼び出す
- `sns llm route list --json` がコマンドとして存在し、API を呼び出す
- `sns llm route set --platform x --provider openai --model gpt-4o` がコマンドとして存在する
- `sns skills list --json` がコマンドとして存在し、API を呼び出す
- `sns skills pack --platform x --provider codex` がコマンドとして存在する
- API 未実装の場合、適切なエラーメッセージが表示され終了コード 1 を返す
- 全コマンドで `--json` と `--help` がサポートされている
- `pnpm build --filter @sns-agent/cli` がエラーなく完了する
