# Task 3001 done_when

- `pnpm build --filter @sns-agent/cli` がエラーなく完了する
- `sns --help` でヘルプが表示される
- `sns accounts list --json` が JSON 形式でアカウント一覧を stdout に出力する
- `sns accounts list` がテーブル形式でアカウント一覧を stdout に出力する
- `sns accounts list --platform x` で X アカウントのみフィルタされる
- `sns accounts show <id> --json` がアカウント詳細を JSON で出力する
- `sns accounts connect x` が OAuth URL を表示する
- API 接続エラー時に終了コード 1 が返り、stderr にエラーメッセージが出力される
- `--json` 指定時のエラーが JSON 形式で stdout に出力される
- 環境変数 `SNS_API_URL`, `SNS_API_KEY` で接続先を設定できる
