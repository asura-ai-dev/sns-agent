# Task 2006 done_when

- `GET /api/posts?platform=x` で X の投稿のみが返る
- `GET /api/posts?status=draft,scheduled` で下書きと予約済みの投稿のみが返る
- `GET /api/posts?from=2026-04-01&to=2026-04-30` で日付範囲内の投稿のみが返る
- `GET /api/posts?page=2&limit=10` でページネーションが正しく動作し、meta に total, totalPages が含まれる
- 投稿オブジェクトに socialAccount の displayName と platform が含まれている
- 予約済み投稿に schedule 情報（scheduledAt, status）が含まれている
- 統合テスト（投稿作成 -> バリデーション -> 公開フロー）が vitest で pass する
- `pnpm test` がエラーなく完了する
