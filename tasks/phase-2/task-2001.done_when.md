# Task 2001 done_when

- API キー付きリクエスト（`Authorization: Bearer <key>`）で認証が成功し、actor 情報が context にセットされる
- API キーなし・不正キーのリクエストで 401 が返る
- `requirePermission('post:create')` を適用したエンドポイントで viewer ロールが 403 を返す
- `requirePermission('post:create')` を適用したエンドポイントで operator ロールが通過する
- `packages/core/src/usecases/auth.ts` に resolveActorByApiKey, resolveActorByUserId が実装されている
- `packages/db/src/repositories/` に UserRepository, AgentIdentityRepository の実装が存在する
- `packages/db/src/seed.ts` が存在し、実行するとデフォルトワークスペース・owner ユーザー・agent identity が作成される
- `pnpm build` がエラーなく完了する
