# Task 2003 done_when

- `packages/provider-x/src/index.ts` が SocialProvider インターフェースを実装するクラスを export している
- `getCapabilities()` が X の capability オブジェクトを返す
- `validatePost` がテキスト 280 文字超過時にバリデーションエラーを返す
- `publishPost` が X API v2 の POST /2/tweets を呼び出す実装になっている（モック可）
- `deletePost` が X API v2 の DELETE /2/tweets/:id を呼び出す実装になっている
- OAuth 2.0 PKCE の認可 URL 生成、コード交換、トークンリフレッシュが実装されている
- X API のエラーレスポンスが ProviderError に変換される
- `packages/core/src/interfaces/provider-registry.ts` に ProviderRegistry が定義されている
- `apps/api/src/providers.ts` で X プロバイダが登録されている
- `pnpm build --filter @sns-agent/provider-x` がエラーなく完了する
